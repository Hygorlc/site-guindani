import { getProductsDb } from "./_products.js"; export default async function handler(req: any, res: any) { if (req.method !== "GET") { res.setHeader("Allow", "GET"); return res.status(405).json({ error: "Method not allowed" }); } try { const sql = await getProductsDb(); const category = req.query.category as string | undefined; const rows = category ? await sql`SELECT id, category, image_url, description FROM products WHERE category = ${category} ORDER BY created_at DESC` : await sql`SELECT id, category, image_url, description FROM products ORDER BY created_at DESC`; return res.status(200).json({ products: rows }); } catch (err) { console.error(err); return res.status(500).json({ error: "Erro ao listar produtos" }); } }
