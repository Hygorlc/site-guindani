import { neon } from "@neondatabase/serverless"; const sql = neon(process.env.DATABASE_URL as string); let ensured = false; export async function getProductsDb() { if (!ensured) { await sql`CREATE TABLE IF NOT EXISTS products (id SERIAL PRIMARY KEY, category TEXT NOT NULL, image_url TEXT NOT NULL, description TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now())`; ensured = true; } return sql; }
